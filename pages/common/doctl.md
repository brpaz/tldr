# doctl

> DigitalOcean client

- Authenticate:

`doctl auth init`

- List droplets:

`doctl compute droplet list`

- Create droplet:

`doctl compute droplet create test --size 1gb --image debian-8-x64 --region nyc1`

